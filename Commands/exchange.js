/*const convertCurrency = require('nodejs-currency-converter');
module.exports = {
    name: 'exchange',
    description: "Exchanges money",
    async execute(Client, message, args, Discord, cmd){
        convertCurrency(1, 'USD', 'BRL').then(response => condole.log(response));
    }
}*/
//Does not work, need to find another thing